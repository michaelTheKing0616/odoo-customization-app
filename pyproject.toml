[project]
name = "odoo-customization-workspace"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[tool.uv.workspace]
members = ["apps/api", "packages/odoo-client", "packages/module-generator"]
