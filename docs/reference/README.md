# Master reference (Document 8)

Place the **8-document master reference** bundle here as `MASTER_REFERENCE.md` when available.

EXP-1 ingestion reads this file when present (tagged `source: project`, `version: all`).
Do not reconstruct from scratch — provide the verified reference from the product owner.

Until then, capability docs under `docs/*.md` are ingested automatically.
