import { describe, expect, it } from "vitest";
import { DesignerReportingFieldsPanel } from "./DesignerReportingFieldsPanel";

describe("DesignerReportingFieldsPanel", () => {
  it("exports a component function", () => {
    expect(typeof DesignerReportingFieldsPanel).toBe("function");
  });
});
