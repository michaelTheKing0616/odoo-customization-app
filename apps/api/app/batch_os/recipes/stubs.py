"""Legacy stub builder — Batch OS series complete; returns empty registry."""

from __future__ import annotations

from typing import Any


STUB_SPECS: list[tuple[str, str, str, str]] = []


def build_stubs() -> dict[str, Any]:
    return {}
