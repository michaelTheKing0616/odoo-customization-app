# App API (Phase 7)
# AUTH_MODE=off          # local gates / tests (default)
# AUTH_MODE=api_key      # require Bearer / X-API-Key
# APP_API_KEY=oc_change_me
# RATE_LIMIT_PER_MINUTE=120

# Local Odoo gate credentials (never commit real customer secrets).
ODOO_URL=http://127.0.0.1:8069
ODOO_DB=odoo_dev
ODOO_USER=admin
ODOO_PASSWORD=admin
